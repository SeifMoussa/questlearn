One row of the persistent left sidebar nav (teacher and learner shells both use it). Render on the dark indigo sidebar background only.

```tsx
<SidebarNavItem icon="🏠" label="Overview" active />
<SidebarNavItem icon="🔔" label="Notifications" badge={3} />
```
