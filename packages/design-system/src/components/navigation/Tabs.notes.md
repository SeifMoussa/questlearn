Underline tab row — class detail (Overview / Learners / Assignments / Quests / Reports / Settings).

```tsx
<Tabs items={['Overview','Learners','Assignments','Reports']} active={tab} onChange={setTab} />
```
