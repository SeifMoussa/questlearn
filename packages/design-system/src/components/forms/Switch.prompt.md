On/off toggle with an optional label + description — question settings ("Shuffle Answers", "Time Limit").

```tsx
<Switch checked={shuffle} onChange={setShuffle} label="Shuffle Answers" description="Randomize order for each student." />
```
