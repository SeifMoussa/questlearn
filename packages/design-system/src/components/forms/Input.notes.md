Text field / textarea — question text, explanation, hint. Set `multiline` for the textarea variant (used with a character counter via `maxLength`).

```tsx
<Input multiline maxLength={500} value={question} onChange={setQuestion} placeholder="Question" />
```
